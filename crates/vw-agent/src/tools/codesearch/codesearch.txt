- 在当前工作区内搜索与信息需求最相关的代码位置与片段
- 适合输入自然语言描述（"入口在哪里""谁调用了 X""哪里定义了 Y"），会自动提取关键词并搜索
- 输出按相关度排序的文件列表，并附上命中行的行号与内容片段

使用说明：
  - 对于精确字符串（路径、标识符、import 路径等）效果最好
  - 可以通过 target_directories 限定搜索目录以加速
  - 结果有 head_limit 上限，默认 20 个文件
